# Tabelas fiscais (NCM / CFOP)

- `ncm.json` — códigos NCM de 8 dígitos (Siscomex Classif)
- `cfop.json` — tabela CFOP vigente (CONFAZ via br-validators)

Atualizar:

```bash
npm run fiscal:refresh
```
